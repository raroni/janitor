#!/usr/bin/env coffee

exec = require('child_process').exec

exec 'pwd', (error, stdout, stderr) ->
  dir = [stdout.replace("\n", ""), 'test'].join '/'
  Runner = require '../src/node_runner'
  runner = new Runner {dir}
  runner.run()
